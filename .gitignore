# Build artifacts
**/.vs/**
**/obj/**
**/bin/**

# Node
**/node_modules/
**/.angular/
**/dist/

# Runtime-uploaded user content (served from wwwroot/uploads)
cinemabe/**/wwwroot/uploads/

# Stray accidental nested duplicate of entity files — safe to delete
cinemabe/Cinema/cinemabe/

# Environment-specific secrets — connection strings, etc. (never commit)
**/appsettings.Development.json
**/appsettings.Production.json
.claude/**
QA-tests/
CLAUDE.md
.playwright-mcp/
pagelist.txt
